# ============================================================
# rd-knowledge-svc 依赖（生产）
#
# 版本与项目一 rd-agent-platform 对齐，已验证跑通。
# 测试依赖在 requirements-dev.txt；安装：pip install -r requirements.txt -r requirements-dev.txt
# ★ pymupdf / litellm 是隐性必需：前者被 pipeline 的表格/公式原图裁剪延迟导入，
#   缺失时该功能静默关闭；后者是 trulens litelern provider 的底层依赖。
# ============================================================

# ---------------- Web 框架 ----------------
fastapi==0.135.1
uvicorn==0.42.0
python-multipart==0.0.22
sse-starlette==3.3.4
httpx==0.28.1

# ---------------- 配置与日志 ----------------
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2
loguru==0.7.3

# ---------------- PostgreSQL ----------------
SQLAlchemy==2.0.48
asyncpg==0.30.0
greenlet==3.3.2
alembic==1.18.4

# ---------------- Neo4j ----------------
neo4j==5.28.2

# ---------------- Milvus ----------------
pymilvus==2.6.10

# ---------------- Redis ----------------
redis==7.3.0

# ---------------- 可观测性 ----------------
prometheus-client==0.25.0

# ---------------- LangChain ----------------
langchain==1.2.15
langchain-core==1.2.28
langchain-community==0.4.1
langchain-experimental==0.4.1
langchain-openai==1.1.12
dashscope==1.25.16

# ---------------- MinIO + 文档解析 ----------------
minio==7.2.18
pypdf==6.10.0
pymupdf==1.26.7        # 表格/公式 bbox 原图裁剪（缺失时该功能静默关闭）
llama-index-core>=0.12.0   # 解析降级链兜底（SimpleDirectoryReader）；MinerU 为主解析，此为 fallback 依赖

# ---------------- 评测（TruLens）----------------
trulens>=2.9.0                        # meta 包：trulens.core / trulens.apps / dashboard
trulens-providers-litellm>=2.9.0      # litellm 裁判 provider（含 litellm 传递依赖）

# ---------------- MinerU 轮询 ----------------
aiohttp==3.13.3

# ---------------- JWT ----------------
PyJWT==2.10.1
